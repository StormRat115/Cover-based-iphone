import { updateBlood, drawBlood, resetBlood } from './bloodEffects.js?v=20260905-53';
var canvas=document.getElementById('game'),overlay=document.createElement('canvas');overlay.id='bloodCanvas';overlay.style.cssText='position:fixed;inset:0;width:100vw;height:100vh;pointer-events:none;z-index:2';canvas.parentNode.insertBefore(overlay,canvas.nextSibling);var g=overlay.getContext('2d'),last=performance.now();function resize(){var d=Math.min(devicePixelRatio||1,2);overlay.width=innerWidth*d;overlay.height=innerHeight*d;g.setTransform(d,0,0,d,0,0)}addEventListener('resize',resize);resize();function iso(x,y){var p=window.__battlePlayer;if(!p)return[0,0];var sx=.25,sy=.125,cx=p.x,cy=p.y;return[innerWidth/2+(x-cx-y+cy)*sx,innerHeight/2+(x-cx+y-cy)*sy-40]}function frame(now){var dt=Math.min(.05,(now-last)/1000);last=now;updateBlood(dt);g.clearRect(0,0,innerWidth,innerHeight);drawBlood(g,iso);requestAnimationFrame(frame)}requestAnimationFrame(frame);window.__resetBlood=resetBlood;
