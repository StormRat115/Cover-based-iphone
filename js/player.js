import { getHitChance } from './cover.js';
import { weaponCopy } from './weapons.js';
const sprite=new Image();sprite.src='./assets/player.svg?v=20260904-17';
const DRAW_SCALE=.40;
let chanceHud=null,shotHud=null,weaponHud=null;
function getChanceHud(){if(chanceHud)return chanceHud;chanceHud=document.createElement('div');chanceHud.id='hitChance';chanceHud.style.cssText='position:fixed;left:50%;top:calc(env(safe-area-inset-top) + 72px);transform:translateX(-50%);padding:5px 9px;border:1px solid #ffffff33;border-radius:8px;background:#11181dcc;color:#9edcff;font:900 12px system-ui;letter-spacing:1px;text-shadow:0 2px 4px #000;z-index:6;pointer-events:none;display:none;white-space:nowrap';document.body.appendChild(chanceHud);return chanceHud}
function getShotHud(){if(shotHud)return shotHud;shotHud=document.createElement('div');shotHud.id='shotFeedback';shotHud.style.cssText='position:fixed;left:50%;top:calc(50% + 8px);transform:translate(-50%,-50%);padding:4px 8px;border-radius:6px;background:#111a;color:#ff9a82;font:900 13px system-ui;letter-spacing:1.5px;text-shadow:0 2px 4px #000;z-index:7;pointer-events:none;display:none';document.body.appendChild(shotHud);return shotHud}
function getWeaponHud(){if(weaponHud)return weaponHud;weaponHud=document.createElement('div');weaponHud.id='weaponHud';weaponHud.style.cssText='position:fixed;right:12px;bottom:calc(env(safe-area-inset-bottom) + 112px);padding:7px 10px;border:1px solid #ffffff22;border-radius:8px;background:#101714dd;color:#e8eee9;font:900 10px system-ui;letter-spacing:1px;text-align:right;text-shadow:0 2px 4px #000;z-index:5;pointer-events:none';document.body.appendChild(weaponHud);return weaponHud}
function showChance(p){const h=getChanceHud(),e=p.aimTarget;if(!e||e.dead){h.style.display='none';return}const chance=getHitChance(p,e,window.__battleCovers||[])+p.weapon.accuracy;h.textContent='HIT CHANCE  '+Math.max(5,Math.min(98,Math.round(chance)))+'%';h.style.display='block';h.style.color=chance>=70?'#9be27f':chance>=45?'#f2d36b':'#ff8f7a'}
function showShotFeedback(hit){const h=getShotHud();h.textContent=hit?'HIT':'MISS';h.style.color=hit?'#b8f28c':'#ff9a82';h.style.display='block';clearTimeout(h._timer);h._timer=setTimeout(function(){h.style.display='none'},hit?180:420)}
function showWeapon(p){const h=getWeaponHud();h.innerHTML='<div style="color:#9edcff">'+p.weapon.name+'</div><div style="font-size:16px;margin-top:2px">'+p.weapon.ammo+' / '+p.weapon.magazine+'</div>'+(p.reloading?'<div style="color:#f2d36b;font-size:9px;margin-top:2px">RELOADING</div>':'');}
export function createPlayer(){const p={x:0,y:120,tx:0,ty:120,hp:100,maxHp:100,speed:185,state:'idle',anim:0,cover:null,coverTarget:null,coverBlend:0,aimTarget:null,reloading:false,reloadTimer:0,shootTimer:0,peek:0,peekSide:1,hitFlash:0,dead:false,downed:false,downTimer:0,downDuration:12,reviveTimer:0,reviveDuration:2.6,regenDelay:3,regenRate:5,timeSinceDamage:99,weapon:weaponCopy('rifle'),keyboardMove:null,facingX:1,facingY:0,lastShotHit:false,setWeapon:function(id){if(this.dead||this.downed||this.reloading)return;this.weapon=weaponCopy(id)},setDestination:function(x,y,cover){if(this.dead||this.downed)return;this.keyboardMove=null;this.cover=null;this.coverTarget=cover||null;this.tx=x;this.ty=y;this.state='walk'},setKeyboardMove:function(v){if(this.dead||this.downed)return;this.keyboardMove=v;this.cover=null;this.coverTarget=null;this.coverBlend=0;this.tx=this.x;this.ty=this.y;if(v){this.facingX=v.x;this.facingY=v.y;this.state='walk'}else if(this.state==='walk')this.state='idle'},startReload:function(){if(this.dead||this.downed||this.reloading||this.weapon.ammo===this.weapon.magazine)return;this.reloading=true;this.reloadTimer=this.weapon.reload;this.state='reload'},fireAt:function(enemy){if(this.dead||this.downed||this.reloading||this.weapon.ammo<=0||this.weapon.fireCooldown>0)return false;const dx=enemy.x-this.x,dy=enemy.y-this.y,d=Math.hypot(dx,dy)||1;this.facingX=dx/d;this.facingY=dy/d;const chance=Math.max(5,Math.min(98,getHitChance(this,enemy,window.__battleCovers||[])+this.weapon.accuracy));enemy.lastHitChance=chance;this.weapon.ammo--;this.weapon.fireCooldown=this.weapon.cooldown;this.weapon.recoil=this.weapon.cooldown;this.state='shoot';this.shootTimer=Math.min(.22,this.weapon.cooldown);this.peek=.22;this.peekSide=this.cover?(Math.abs(enemy.x-this.cover.x)>=Math.abs(enemy.y-this.cover.y)?(enemy.x<this.cover.x?-1:1):(enemy.y<this.cover.y?-1:1)):1;const hit=Math.random()*100<chance;this.lastShotHit=hit;showShotFeedback(hit);if(hit){enemy.hp-=this.weapon.damage;if(enemy.hp<=0){enemy.hp=0;enemy.dead=true;enemy.deathTimer=0}}return hit},takeDamage:function(amount){if(this.dead||this.downed)return;this.hp=Math.max(0,this.hp-amount);this.timeSinceDamage=0;this.hitFlash=.18;if(this.hp<=0)this.triggerDowned()},triggerDowned:function(){if(this.dead||this.downed)return;this.downed=true;this.downTimer=0;this.reviveTimer=0;this.state='downed';this.reloading=false;this.shootTimer=0;this.peek=0;this.keyboardMove=null;this.cover=null;this.coverTarget=null},revive:function(){if(this.dead)return false;this.downed=false;this.hp=Math.max(35,Math.round(this.maxHp*.4));this.downTimer=0;this.reviveTimer=0;this.timeSinceDamage=0;this.state='idle';return true},triggerDeath:function(){if(this.dead)return;if(this.downed){this.dead=true;this.downed=false;this.state='dead';this.deathTimer=0;return}this.triggerDowned()},reset:function(){Object.assign(this,{x:0,y:120,tx:0,ty:120,hp:100,state:'idle',anim:0,cover:null,coverTarget:null,coverBlend:0,aimTarget:null,reloading:false,reloadTimer:0,shootTimer:0,peek:0,peekSide:1,hitFlash:0,dead:false,downed:false,downTimer:0,reviveTimer:0,timeSinceDamage:99,keyboardMove:null,facingX:1,facingY:0,lastShotHit:false});this.weapon=weaponCopy('rifle')},update:function(dt){this.anim+=dt;if(this.dead){this.deathTimer=Math.min(this.deathDuration,this.deathTimer+dt);showChance(this);showWeapon(this);return}this.hitFlash=Math.max(0,this.hitFlash-dt);this.weapon.fireCooldown=Math.max(0,this.weapon.fireCooldown-dt);this.weapon.recoil=Math.max(0,this.weapon.recoil-dt);this.peek=Math.max(0,this.peek-dt*2.8);this.timeSinceDamage+=dt;showChance(this);showWeapon(this);if(this.downed){this.downTimer+=dt;if(this.downTimer>=this.downDuration){this.dead=true;this.downed=false;this.state='dead';this.deathTimer=0}return}if(this.hp<this.maxHp&&this.timeSinceDamage>=this.regenDelay)this.hp=Math.min(this.maxHp,this.hp+this.regenRate*dt);if(this.shootTimer>0){this.shootTimer-=dt;if(this.shootTimer<=0&&this.state==='shoot')this.state='idle'}if(this.reloading){this.reloadTimer-=dt;if(this.reloadTimer<=0){this.reloading=false;this.weapon.ammo=this.weapon.magazine;this.state='idle'}}if(this.reloading)return;if(this.keyboardMove){this.x+=this.keyboardMove.x*this.speed*dt;this.y+=this.keyboardMove.y*this.speed*dt;this.tx=this.x;this.ty=this.y;this.state='walk';return}const dx=this.tx-this.x,dy=this.ty-this.y,d=Math.hypot(dx,dy);if(d>4){this.facingX=dx/d;this.facingY=dy/d;this.x+=dx/d*this.speed*dt;this.y+=dy/d*this.speed*dt;this.state='walk'}else if(this.state==='walk'){this.x=this.tx;this.y=this.ty;this.cover=this.coverTarget;this.state='idle'}const coverGoal=this.cover?1:0;this.coverBlend+=(coverGoal-this.coverBlend)*Math.min(1,dt*10)}};return p}
export function drawPlayer(ctx,p,iso){let q=iso(p.x,p.y),sx=q[0],sy=q[1],crouch=p.coverBlend;const death=p.dead?1:0;ctx.save();ctx.translate(sx,sy);ctx.globalAlpha=1;if(p.downed){ctx.globalAlpha=.65;ctx.fillStyle='#d85b50';ctx.fillRect(-11,-4,22,3);ctx.fillStyle='#fff';ctx.font='900 9px system-ui';ctx.textAlign='center';ctx.fillText('DOWNED '+Math.max(0,Math.ceil(p.downDuration-p.downTimer))+'s',0,-12)}if(p.dead){ctx.globalAlpha=.92;ctx.rotate(-.12);ctx.translate(0,6)}if(p.hitFlash>0&&!p.dead){ctx.globalAlpha=.6;ctx.fillStyle='#ff4545';ctx.beginPath();ctx.arc(0,-13,13,0,Math.PI*2);ctx.fill();ctx.globalAlpha=p.downed?.65:1}if(!p.downed&&p.cover&&!p.dead){ctx.strokeStyle='#8fb7c8';ctx.lineWidth=1.5;ctx.beginPath();ctx.arc(0,-4,13,Math.PI,Math.PI*2);ctx.stroke()}const frame=p.dead?6:p.downed?5:p.state==='shoot'?4:p.reloading?3:p.cover?3:p.state==='walk'?1+Math.floor(p.anim*8)%2:0;const bob=p.dead||p.downed?0:Math.sin(p.anim*10)*.8*(crouch*.35+(!crouch?1:0));let flip=p.facingX<0?-1:1;ctx.scale(flip,1);const recoil=p.weapon.recoil?Math.sin(p.weapon.recoil/Math.max(.01,p.weapon.cooldown)*Math.PI)*2:0;if(sprite.complete&&sprite.naturalWidth)ctx.drawImage(sprite,frame*64,0,64,64,-32*DRAW_SCALE,-64*DRAW_SCALE+bob-recoil,64*DRAW_SCALE,64*DRAW_SCALE);else{ctx.fillStyle='#4e626d';ctx.fillRect(-8*DRAW_SCALE,-38*DRAW_SCALE,16*DRAW_SCALE,36*DRAW_SCALE);ctx.fillStyle='#9a9d9b';ctx.beginPath();ctx.arc(0,-48*DRAW_SCALE,10*DRAW_SCALE,0,Math.PI*2);ctx.fill()}ctx.restore()}
